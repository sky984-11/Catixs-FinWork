# AGENTS.md

本文件适用于仓库根目录及其子目录；如子目录存在更具体的 `AGENTS.md`，以子目录规则为准。

## 项目结构

- `app/`：FastAPI 后端、业务逻辑与数据模型。
- `web/`：Vue 3 + Vite 前端。
- `migrations/`：Aerich 数据库迁移文件。
- `scripts/`：一次性或运维辅助脚本。
- `docs/`：项目文档；`uploads/`、`backups/` 视为运行数据，非必要不修改。

## 通用约定

- 先阅读相关代码与既有模式，再做最小、聚焦的修改；不要顺带重构无关模块。
- 保持现有 API、字段名、路由和数据库兼容性；若必须变更，说明影响并同步调用方。
- 不提交或展示 `.env`、密钥、令牌、数据库导出或用户上传文件。
- 不使用破坏性 Git 命令（如 `reset --hard`）；保留用户已有的未提交改动。
- 不手工修改已生成或历史迁移文件；数据库结构调整应新增 Aerich 迁移并审阅迁移内容。

## 后端（Python）

- 使用 Python 3.11+，遵循现有 FastAPI、Pydantic 和 Tortoise ORM 的组织方式。
- 格式与静态检查遵循 `pyproject.toml`：行宽 120；优先使用 `black`、`isort` 与 `ruff`。
- 新增或修改接口时，兼顾鉴权、输入校验、异常处理、日志以及响应模型；同步更新 API 文档，注明路由、请求参数、响应示例、权限与错误码。
- 涉及异步数据库操作时使用 `await`，避免在请求路径中引入阻塞 I/O。

## 前端（Vue）

- 使用 Vue 3、Vite、Pinia、Vue Router 和 Naive UI 的既有规范；复用现有组件、请求封装和样式方案。
- 弹窗（Modal、Dialog、Drawer 等）中的操作按钮统一复用项目 `CButton` 组件，不混用原生按钮或第三方按钮组件；保持已有的按钮文案、类型和加载态规范。
- 列表和表格中的操作按钮风格统一参考“客户管理”页面：使用项目既有图标按钮样式，按钮文案通过悬浮提示展示。
- 新增或修改表格时，业务列默认启用拖拽调整列宽；操作列保持固定宽度。列总宽度超过容器时，启用横向滚动条。
- 不随意增加依赖或全局样式；新增依赖须有明确必要性。
- 修改页面时注意加载、空数据、错误状态，以及窄屏下的基本可用性。


## 验证

- 后端改动至少执行相关模块的语法检查或测试；可用时运行：`python -m py_compile <文件>`。
- 前端改动至少执行：`cd web; npm run build`；如修改了 JS/Vue 逻辑，也运行 `npm run lint`。
- 新增或变更接口时，确认 API 文档已同步；迁移、鉴权、导入或删除相关改动须进行针对性验证，并在交付时说明已运行的命令和未验证项。

## 交付说明

- 简要说明改了什么、涉及哪些文件，以及验证结果。
- 发现无关的既有问题时单独指出，不在未获确认的情况下修复。

## 问答规范

目标：在信息充分时直接给出可执行答案；在信息不足时以最少轮次消除关键歧义，并避免无关展开。

### 回答原则

- 先给结论、决定或下一步，再补充必要依据；不要复述用户的问题。
- 只回答当前问题及完成任务所必需的关联内容；不罗列未被要求的备选方案、背景知识或泛泛提示。
- 将事实、代码/文档依据和推测明确区分；不确定时说明不确定点及其影响，不编造细节。
- 优先检查仓库代码、配置、文档和已给上下文；只有时效性、外部事实或用户明确要求时再检索外部来源。
- 涉及接口、配置、命令或代码时，给出精确名称、路径、参数和可复制命令；示例保持最小可用。
- 发现请求存在高风险或会造成兼容性变化时，先指出具体影响；缺少决定性信息才提出一个简短、明确的问题。

### 节省 Token

- 默认使用简短段落或少量列表；仅在比较、步骤依赖或字段映射复杂时使用表格。
- 每个结论只保留一条最相关依据，避免同义重复和长篇引用。
- 修改代码后的交付使用固定格式：`结果`、`涉及文件`、`验证`、`注意事项（仅有时）`。
- 除非用户要求，不输出完整文件、完整日志、冗长推理过程或已可从代码中直接读取的大段内容。
- 对连续追问复用已确认的上下文；若前提发生变化，明确指出变化而非重新完整说明。

### 推荐答复模板

```text
结果：<直接结论或已完成事项>
依据：<最关键的代码、文档或验证结果>
下一步：<仅在用户需要操作或仍有阻塞时提供>
```
